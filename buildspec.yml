version: 0.2

phases:
  install:
    commands:
      - npm install
  post_build:
    commands:
      - npm prune
      - npm install -g redoc-cli
      - npx serverless deploy
      - redoc-cli bundle api-oas30-postman.json
      - aws s3 cp --sse AES256 redoc-static.html s3://oceanarchive-demo-client/api.html
      